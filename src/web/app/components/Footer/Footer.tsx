import styled from 'styled-components';

import footer from './footer.png';

const Footer = styled.footer`
  display: block;

  width: 100%;
  height: 140px;

  background: url(${footer}) repeat-x;
`;

export default Footer;
