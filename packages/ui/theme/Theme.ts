export interface Theme {
  primaryFont: string;

  primary: string;
  primaryHover: string;
  primaryActive: string;

  textPrimary: string;
  textSecondary: string;
  textDisabled: string;

  border: string;
  borderRadius: string;

  separator: string;
}
